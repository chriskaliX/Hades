egress:
  - name: "eguard_egress_test_project"
    address: "192.168.1.1/24"
    protocol: TCP # ALL/TCP/UDP
    ports:        # 0 or empty means match all ports, port range will be considered
      - 80
    action: DENY  # DENY/LOG
    level: INFO