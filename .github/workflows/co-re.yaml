# driver ci build
name: CI - edriver
on:
  push:
    branches: ["**"]
    paths:
      - "plugins/edriver/**"
      - "SDK/rust/**"
      - ".github/workflows/co-re.yaml"
  pull_request:
    paths:
      - "plugins/edriver/**"
      - "SDK/rust/**"
      - ".github/workflows/co-re.yaml"

jobs:
  edriver:
    name: "Build and test edriver"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - name: "Git checkout"
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-musl

      - name: Cache cargo registry & build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            plugins/edriver/target
          key: ${{ runner.os }}-cargo-edriver-${{ hashFiles('plugins/edriver/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-edriver-

      - name: Install build dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            build-essential pkgconf libelf-dev llvm-14 clang-14 \
            musl-tools libzstd-dev protobuf-compiler
          for tool in clang llc llvm-strip
          do
            sudo rm -f /usr/bin/$tool
            sudo ln -s /usr/bin/${tool}-14 /usr/bin/$tool
          done

      - name: Build
        run: |
          export PROTOC=$(which protoc)
          cd plugins/edriver
          make build
        env:
          LIBBPF_SYS_LIBRARY_PATH: /usr/lib/x86_64-linux-gnu

      - name: Test
        run: |
          export PROTOC=$(which protoc)
          cd plugins/edriver
          make test
        env:
          LIBBPF_SYS_LIBRARY_PATH: /usr/lib/x86_64-linux-gnu
